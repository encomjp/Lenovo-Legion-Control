# Legion Control — system group for daemon IPC socket access.
# The root daemon binds /run/legion-control.socket with mode 0660 owned by
# this group; CLI/GUI/widget clients must be a member (or root) to connect.
g legion - -
