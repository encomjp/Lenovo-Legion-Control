# Legion Control color SVG icon preview — v2

12 flat-color SVG candidates. App Mark and Tray were redesigned. No files in `lenovo-legion-tool` were changed.

Open `index.html` to review all icons together.
